// TODO this is a template that can be used for our classes-needs new attributes, etc...
export class User {
	constructor(public id: number, public email: string, public phone: string, public name: string) {}
}